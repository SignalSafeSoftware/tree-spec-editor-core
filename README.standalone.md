# Standalone export

This tree was generated by `scripts/export-standalone-npm-package.sh tree-spec-editor-core` from the DeliveryPlus monorepo.

```bash
git init
git branch -M main
git add .
git commit -m "Sync tree-spec-editor-core from DeliveryPlus"
git remote add origin git@github.com:SignalSafeSoftware/tree-spec-editor-core.git
git push -u origin main
```

See `RELEASING.md` for npm publish steps.
